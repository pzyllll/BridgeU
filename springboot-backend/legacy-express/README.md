Legacy Express backend moved under `springboot-backend/legacy-express`.

## 使用说明

```bash
cd springboot-backend/legacy-express
npm install
npm run seed
npm run dev
```

API 端口默认 `http://localhost:4000`，与原 `backend` 目录保持一致。

